import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.watertracker',
  appName: 'Water Tracker',
  webDir: 'build'
};

export default config;
